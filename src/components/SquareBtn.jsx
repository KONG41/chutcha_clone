import React from 'react'

const SquareBtn = ({ text, icon }) => {

  return (
    <div className="square-btn store-btn">
      <span className="icon">{icon}</span>
    </div>
  )
}

export default SquareBtn